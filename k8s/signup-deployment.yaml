apiVersion: apps/v1
kind: Deployment
metadata:
  name: signup-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: signup-app
  template:
    metadata:
      labels:
        app: signup-app
    spec:
      containers:
      - name: signup-app
        image: list-app:1.1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5000
        volumeMounts:
        - name: data
          mountPath: /data
      volumes:
      - name: data
        emptyDir: {}
